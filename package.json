{
  "name": "guilleojeda-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "check-output": "node scripts/check-output.mjs",
    "verify": "npm run check && npm run test && npm run build && npm run check-output"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.6.2",
    "astro": "^7.3.3",
    "typescript": "^6.0.3"
  }
}
